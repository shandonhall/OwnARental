{
  "framework": "nextjs",
  "installCommand": "npm install",
  "buildCommand": "npm run prisma:generate && npm run build -w web && node -e \"require('fs').rmSync('.next',{recursive:true,force:true});require('fs').cpSync('apps/web/.next','.next',{recursive:true})\""
}
